<h2>Edit Income</h2>

<%= render "form.html", Map.put(assigns, :action, Routes.budget_income_path(@conn, :update, @budget, @income)) %>

<%= link "Back", to: Routes.budget_path(@conn, :show, @budget), class: "btn btn-secondary btn-sm" %>
