<h2>New Income</h2>

<%= render "form.html", Map.put(assigns, :action, budget_income_path(@conn, :create, @budget)) %>

<%= link "Back", to: budget_path(@conn, :index), class: "btn btn-secondary btn-sm" %>
