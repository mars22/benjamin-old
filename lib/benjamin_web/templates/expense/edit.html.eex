<h2>Edit Expense</h2>

<%= render "form.html", Map.put(assigns, :action, expense_path(@conn, :update, @expense)) %>

<%= link "Back", to: expense_path(@conn, :index), class: "btn btn-secondary btn-sm" %>
