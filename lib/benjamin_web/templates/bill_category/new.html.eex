<h2>New Bill category</h2>

<%= render "form.html", Map.put(assigns, :action, Routes.bill_category_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.bill_category_path(@conn, :index) %></span>
