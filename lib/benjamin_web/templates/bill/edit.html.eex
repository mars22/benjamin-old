<h2>Edit Bill</h2>

<%= render "form.html", Map.put(assigns, :action, Routes.budget_bill_path(@conn, :update, @budget, @bill)) %>

<%= link "Back", to: Routes.budget_path(@conn, :show, @budget), class: "btn btn-secondary btn-sm" %>
