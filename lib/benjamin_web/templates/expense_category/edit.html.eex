<h2>Edit Expense category</h2>

<%= render "form.html", Map.put(assigns, :action, Routes.expense_category_path(@conn, :update, @expense_category)) %>

<span><%= link "Back", to: Routes.expense_category_path(@conn, :index) %></span>
